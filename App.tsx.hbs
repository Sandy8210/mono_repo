import { Card, CardHeader, CardTitle, CardContent } from "@company/ui"; import {
DynamicFormBuilder } from "@company/form-builder"; import { formSchema } from
"@/form/createForm"; function App() { const handleFormSubmit = (data: any) => {
alert("Form Successfully Validated! Check browser console.");
console.log("Submitted form data:", data); }; return (
<div
  className="flex min-h-screen items-center justify-center bg-background p-4"
>
  <Card className="w-full">
    <CardHeader>
      <CardTitle
        className="text-2xl font-bold text-primary uppercase"
      >{{APP_NAME}} Portal</CardTitle>
    </CardHeader>
    <CardContent className="space-y-6">
      <p className="text-sm text-foreground/80">Independent Vite app rendering
        the dynamic alpha-numeric validation builder.</p>
      <DynamicFormBuilder schema={formSchema} onSubmit={handleFormSubmit} />
    </CardContent>
  </Card>
</div>
) } export default App;